spring:
  application:
    name: user-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/tings_db
    username: root
    password: 123qwerty
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

